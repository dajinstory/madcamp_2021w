<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/contact_image"
        android:layout_width="180dp"
        android:layout_height="240dp"
        android:layout_marginTop="80dp"
        android:contentDescription="contact_image"

        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@mipmap/ic_launcher"
        tools:layout_editor_absoluteY="12dp" />

    <EditText
        android:id="@+id/contact_name"
        android:layout_width="120dp"
        android:layout_height="30dp"
        android:layout_marginTop="30dp"
        android:textSize="20sp"
        android:textStyle="bold"
        android:background = "@drawable/edge"

        app:layout_constraintHorizontal_chainStyle="spread"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@+id/contact_phone_number"
        app:layout_constraintTop_toBottomOf="@+id/contact_image"
        tools:text="Name" />

    <EditText
        android:id="@+id/contact_phone_number"
        android:layout_width="120dp"
        android:layout_height="30dp"
        android:layout_marginTop="30dp"
        android:textSize="20sp"
        android:textStyle="bold"
        android:background = "@drawable/edge"

        app:layout_constraintLeft_toRightOf="@+id/contact_name"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/contact_image"
        tools:text="Phone Number" />

    <EditText
        android:id="@+id/contact_email"
        android:layout_width="120dp"
        android:layout_height="30dp"
        android:layout_marginTop="30dp"
        android:textSize="20sp"
        android:textStyle="bold"
        android:background = "@drawable/edge"

        app:layout_constraintHorizontal_chainStyle="spread"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@+id/contact_memo"
        app:layout_constraintTop_toBottomOf="@+id/contact_name"
        tools:text="Email" />

    <EditText
        android:id="@+id/contact_memo"
        android:layout_width="120dp"
        android:layout_height="30dp"
        android:layout_marginTop="30dp"
        android:textSize="20sp"
        android:textStyle="bold"
        android:background = "@drawable/edge"

        app:layout_constraintLeft_toRightOf="@+id/contact_email"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/contact_name"
        tools:text="Memo" />

</androidx.constraintlayout.widget.ConstraintLayout>